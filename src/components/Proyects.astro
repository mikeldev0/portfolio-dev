---
import DesktopIcon from "../icons/DesktopIcon.astro";
import GitHubIcon from "../icons/GitHubIcon.astro";
import SocialPill from "./SocialPill.astro";
import { env } from "../lib/env";
const { githubToken: GITHUB_TOKEN, githubUrl: GITHUB_URL } = env;
interface Proyect {
    title: string;
    description: string;
    link?: string;
    github?: string;
    image?: string;
    tags: Tag[];
}

interface Tag {
    name: string;
    color: string;
}

interface GitHubRepository {
    name: string;
    description?: string;
    url: string;
    homepageUrl?: string;
    openGraphImageUrl?: string;
    languages: {
        nodes: Array<{ name: string }>;
    };
}

const TAG = {
    JAVA: {
        name: "Java",
        color: "bg-red-600", // Rojo tradicionalmente asociado con Java
    },
    PYTHON: {
        name: "Python",
        color: "bg-blue-400", // Azul claro asociado con el logo de Python
    },
    JAVASCRIPT: {
        name: "JavaScript",
        color: "bg-yellow-600", // Amarillo tradicionalmente asociado con JavaScript
    },
    BASH: {
        name: "Bash",
        color: "bg-black", // Negro asociado con la terminal
    },
    BATCH: {
        name: "Batch",
        color: "bg-gray-600", // Gris oscuro para scripts batch
    },
    HTML: {
        name: "HTML",
        color: "bg-orange-500", // Naranja asociado con el logo de HTML5
    },
    CSS: {
        name: "CSS",
        color: "bg-blue-600", // Azul tradicional asociado con CSS
    },
    SASS: {
        name: "SASS",
        color: "bg-pink-600", // Rosa asociado con el logo de SASS
    },
    TYPESCRIPT: {
        name: "TypeScript",
        color: "bg-blue-800", // Azul oscuro asociado con el logo de TypeScript
    },
    REACT: {
        name: "React",
        color: "bg-cyan-400", // Cian asociado con el logo de React
    },
    ANGULAR: {
        name: "Angular",
        color: "bg-red-700", // Rojo oscuro asociado con el logo de Angular
    },
    SPRING_BOOT: {
        name: "Spring Boot",
        color: "bg-green-600", // Verde asociado con el logo de Spring Boot
    },
    NODE_JS: {
        name: "Node.js",
        color: "bg-green-500", // Verde tradicional asociado con Node.js
    },
    EXPRESS: {
        name: "Express",
        color: "bg-gray-500", // Gris claro asociado con el minimalismo de Express
    },
    MYSQL: {
        name: "MySQL",
        color: "bg-blue-300", // Azul claro asociado con el logo de MySQL
    },
    DOCKER: {
        name: "Docker",
        color: "bg-blue-700", // Azul oscuro asociado con el logo de Docker
    },
    GITHUB: {
        name: "GitHub",
        color: "bg-gray-800", // Gris oscuro tradicionalmente asociado con GitHub
    },
    GITLAB: {
        name: "GitLab",
        color: "bg-orange-600", // Naranja asociado con el logo de GitLab
    },
    JIRA: {
        name: "Jira",
        color: "bg-blue-500", // Azul tradicionalmente asociado con Jira
    },
    BDD: {
        name: "BDD",
        color: "bg-red-500", // Rojo asociado con desarrollo basado en comportamiento
    },
    ASTRO: {
        name: "Astro",
        color: "bg-purple-600", // Morado asociado con el logo de Astro
    },
    PHP: {
        name: "PHP",
        color: "bg-indigo-700", // Azul índigo asociado con el logo de PHP
    },
    VB_NET: {
        name: "VB.NET",
        color: "bg-purple-500", // Morado asociado con Visual Studio
    },
    ANDROID: {
        name: "Android",
        color: "bg-green-700", // Verde tradicional asociado con el logo de Android
    },
    IOS: {
        name: "iOS",
        color: "bg-gray-400", // Gris claro asociado con el logo de iOS
    },
};

// Lista de proyectos estáticos como respaldo
const STATIC_PROYECTS: Proyect[] = [
    {
        link: "https://proyectosjs.mikeldev.com",
        title: "Proyectos desarrollados 100% con JavaScript",
        description:
            "Aquí encontrarás una selección de proyectos desarrollados con JavaScript, HTML y CSS. Desde aplicaciones web hasta juegos, pasando por chatbots y aplicaciones móviles. ¡Descúbrelos!",
        tags: [TAG.JAVASCRIPT, TAG.HTML, TAG.CSS, TAG.GITHUB, TAG.ASTRO],
    },
    {
        title: "Woof & Work Marketing | Agencia de Marketing para Mascotas",
        description:
            "Agencia de marketing digital especializada en negocios del sector de mascotas. Impulsa tu negocio veterinario o tienda de mascotas con estrategias personalizadas.",
        tags: [TAG.REACT, TAG.TYPESCRIPT, TAG.CSS, TAG.GITHUB],
        link: "https://woofandwork.netlify.app",
    },
    {
        title: "WebLLM-local",
        description:
            "Este proyecto es un chatbot impulsado por inteligencia artificial, diseñado para proporcionar respuestas rápidas y precisas a las preguntas de los usuarios. Utiliza tecnologías avanzadas como WebGPU para acelerar las operaciones de cómputo, asegurando una experiencia de usuario fluida y eficiente.",
        github: "/WebLLM-local",
        tags: [TAG.JAVASCRIPT, TAG.HTML, TAG.CSS, TAG.GITHUB],
        link: "https://chat.mikeldev.com",
    },
    {
        title: "Hundir la flota",
        description:
            '"Hundir la Flota" es un juego de tablero clásico para dos jugadores: un usuario y el ordenador. Cada jugador tiene un tablero de 10x10 posiciones (A-J, 0-9) donde se colocan los barcos en orientación horizontal o vertical. El objetivo es hundir los barcos del oponente adivinando sus coordenadas.',
        github: "/HundirLaFlota",
        tags: [TAG.JAVA, TAG.GITHUB],
    },
    {
        title: "HugChat Project in Command Line",
        description:
            "HugChat is a project that I made in the subject of Programming in Python, Batch, and SHELL. It connects with the LLM chatbot API using the HuggingFace API. This project is a chatbot that can communicate with you in the command line.",
        github: "/HuggingChatCLI",
        tags: [TAG.PYTHON, TAG.BASH, TAG.BATCH, TAG.GITHUB],
    },
    {
        title: "Cuatrovientos BlaBlarCar App",
        description:
            "Welcome to the Cuatrovientos High School carpooling app – BlaBlarCar! Developed by a group of dedicated students, including Mikel Echeverria, Juan Hidalgo, and Aitor Gorostizu, this app aims to reduce emissions and promote cost-saving by facilitating shared transportation among institute students.",
        github: "/BlaBlaCarCuatrovientos",
        tags: [TAG.JAVA, TAG.ANDROID, TAG.GITHUB],
    },
    {
        title: "App Mail",
        description:
            "App Mail is a project that I made in the subject of Programming in Java. It is a simple email application interface, you can't send emails, but you can see the emails that you have received.",
        github: "/AppMail",
        tags: [TAG.JAVA, TAG.ANDROID, TAG.GITHUB],
    },
];

// Función para obtener los repositorios anclados de GitHub desde el servidor
async function fetchPinnedRepos(username: string): Promise<Proyect[]> {
    try {
        if (!GITHUB_TOKEN) {
            return [];
        }

        const response = await fetch(GITHUB_URL, {
            method: 'POST',
            headers: {
                'Authorization': `bearer ${GITHUB_TOKEN}`,
                'Content-Type': 'application/json',
            },
            body: JSON.stringify({
                query: `{
                    user(login: "${username}") {
                        pinnedItems(first: 6, types: REPOSITORY) {
                            nodes {
                                ... on Repository {
                                    name
                                    description
                                    url
                                    homepageUrl
                                    languages(first: 3) {
                                        nodes {
                                            name
                                        }
                                    }
                                    openGraphImageUrl
                                }
                            }
                        }
                    }
                }`
            }),
        });

        const data = await response.json();

        if (data.errors) {
            if (import.meta.env.DEV) {
                console.error('Error en la API de GitHub:', data.errors);
            }
            return [];
        }

        return data.data.user.pinnedItems.nodes.map((repo: GitHubRepository) => {
            const repoTags = repo.languages.nodes
                .map((lang) => lang.name)
                .map((langName: string) => {
                    const tagKey = Object.keys(TAG).find(key =>
                        TAG[key as keyof typeof TAG].name.toLowerCase() === langName.toLowerCase()
                    );
                    return tagKey ? TAG[tagKey as keyof typeof TAG] : null;
                })
                .filter((tag: Tag | null) => tag !== null);

            repoTags.push(TAG.GITHUB);

            return {
                title: repo.name,
                description: repo.description || `Un repositorio de GitHub llamado ${repo.name}`,
                github: repo.url,
                link: repo.homepageUrl,
                image: repo.openGraphImageUrl,
                tags: repoTags,
            };
        });
    } catch (error) {
        if (import.meta.env.DEV) {
            console.error('Error al obtener repositorios anclados:', error);
        }
        return [];
    }
}

// Intentar obtener proyectos dinámicos, usar los estáticos como respaldo
let PROYECTS: Proyect[] = STATIC_PROYECTS;

try {
    const githubProyects = await fetchPinnedRepos('mikeldev0');
    if (githubProyects && githubProyects.length > 0) {
        PROYECTS = githubProyects;
    }
} catch (error) {
    if (import.meta.env.DEV) {
        console.error('Error al cargar los repositorios anclados, usando proyectos estáticos:', error);
    }
}

const getRepositoryUrl = (github?: string) => {
    if (!github) return undefined;
    return github.startsWith("http") ? github : `https://github.com/mikeldev0${github}`;
};
---

{
    (
        <div class="grid grid-cols-1 gap-4 sm:grid-cols-2">
            {PROYECTS.slice(0, 6).map(
                ({ title, description, link, github, image, tags }) => {
                    const repositoryUrl = getRepositoryUrl(github);
                    const primaryHref = link?.length ? link : repositoryUrl;

                    return (
                    <article class="flex h-full flex-col overflow-hidden rounded-xl border border-zinc-200 bg-white text-zinc-900 transition-colors duration-200 hover:border-indigo-300 dark:border-zinc-800 dark:bg-zinc-950 dark:text-zinc-100 dark:hover:border-indigo-500/60">
                        <div class="flex h-full flex-col p-5">
                            <div class="flex flex-1 flex-col gap-4">
                                <div>
                                    <h3 class="text-lg font-semibold leading-tight text-zinc-950 dark:text-white">
                                        {title}
                                    </h3>
                                    <p class="mt-2 text-sm leading-6 text-zinc-700 dark:text-zinc-300">
                                        {description}
                                    </p>
                                </div>
                                {image && (
                                    <a
                                        href={primaryHref}
                                        target="_blank"
                                        rel="noopener noreferrer"
                                        class="group block overflow-hidden rounded-lg border border-zinc-200 bg-zinc-100 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 focus:ring-offset-white dark:border-zinc-800 dark:bg-zinc-900 dark:focus:ring-offset-zinc-950"
                                    >
                                        <img
                                            src={image}
                                            alt={title}
                                            width={384}
                                            height={192}
                                            loading="lazy"
                                            class="h-44 w-full object-cover transition duration-300 group-hover:scale-[1.02] motion-reduce:transition-none motion-reduce:group-hover:scale-100"
                                        />
                                    </a>
                                )}
                                <div class="flex flex-wrap gap-2">
                                    {tags.map((tag) => (
                                        <span
                                            class={`rounded-md px-2 py-1 text-xs font-medium text-white ${tag.color}`}
                                        >
                                            {tag.name}
                                        </span>
                                    ))}
                                </div>
                            </div>
                            <div class="mt-5 flex flex-wrap gap-3 border-t border-zinc-200 pt-4 dark:border-zinc-800">
                                    {link && (
                                        <a
                                            href={link}
                                            target="_blank"
                                            rel="noopener noreferrer"
                                            class="inline-flex min-h-11 items-center gap-x-2 rounded-lg border border-zinc-300 px-3 py-2 text-sm font-medium text-zinc-800 transition hover:border-indigo-400 hover:text-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 focus:ring-offset-white dark:border-zinc-700 dark:text-zinc-100 dark:hover:border-indigo-400 dark:hover:text-indigo-300 dark:focus:ring-offset-zinc-950"
                                        >
                                            <DesktopIcon class="size-6" />
                                            <span data-i18n="projectsSection.viewDemo">Ver demo</span>
                                        </a>
                                    )}
                                    {repositoryUrl && (
                                        <a
                                            href={repositoryUrl}
                                            target="_blank"
                                            rel="noopener noreferrer"
                                            class="inline-flex min-h-11 items-center gap-x-2 rounded-lg border border-zinc-300 px-3 py-2 text-sm font-medium text-zinc-800 transition hover:border-indigo-400 hover:text-indigo-700 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2 focus:ring-offset-white dark:border-zinc-700 dark:text-zinc-100 dark:hover:border-indigo-400 dark:hover:text-indigo-300 dark:focus:ring-offset-zinc-950"
                                        >
                                            <GitHubIcon class="size-6" />
                                            <span data-i18n="projectsSection.viewOnGitHub">Ver en GitHub</span>
                                        </a>
                                    )}
                            </div>
                        </div>
                    </article>
                )},
            )}
            {PROYECTS.length > 6 && (
                <div class="flex justify-center col-span-1 sm:col-span-2 px-4">
                    <SocialPill href="?tab=repositories" >
                        <span data-i18n="projectsSection.moreProjects">Ver más proyectos</span>
                    </SocialPill>
                </div>
            )}
        </div>
    )
}
